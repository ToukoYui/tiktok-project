<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tiktok.service_user.mapper.UserMapper">

    <!-- 根据用户号和密码查询用户信息 -->
    <select id="selectByUserNameAndPassword" resultType="com.tiktok.service_user.model.entity.User">
        select * from tb_user where username = #{username} and password = #{password} for update
    </select>


<!--    保存新用户-->
    <insert id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id" parameterType="com.tiktok.service_user.model.entity.User">
        insert into tb_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null and id != '' ">
                id,
            </if>
            <if test="username != null and username != '' ">
                username,
            </if>
            <if test="password != null and password != '' ">
                password,
            </if>
        </trim>
        <trim prefix="values(" suffix=")" suffixOverrides=",">
            <if test="id != null and id != '' ">
                #{id},
            </if>
            <if test="username != null and username != '' ">
                #{username},
            </if>
            <if test="password != null and password != '' ">
                #{password},
            </if>
        </trim>
    </insert>


</mapper>
